<!DOCTYPE html>
<html>
<head>
  <title>Add Vehicle</title>
  <style>
    body {
      font-family: 'Segoe UI', Arial, sans-serif;
      background: #f7f7f7;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 500px;
      margin: 40px auto;
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
      padding: 32px;
    }
    h1 {
      text-align: center;
      color: #333;
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    input, label {
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 15px;
    }
    input[type="file"] {
      border: none;
      background: none;
      padding: 2px;
    }
    button {
      background: #1976d2;
      color: #fff;
      border: none;
      padding: 10px 18px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 15px;
      margin-top: 10px;
    }
    .back-btn {
      background: #e53935;
      margin-left: 10px;
    }
    .btn-row {
      display: flex;
      justify-content: flex-start;
      gap: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Add Vehicle</h1>
    <form method="POST" action="/vehicles" enctype="multipart/form-data">
      <input name="vehicleName" placeholder="Vehicle Name" required>
      <input name="price" placeholder="Price" required>
      <input type="file" name="image" accept="image/*" required>
      <input name="desc" placeholder="Description" required>
      <input name="brand" placeholder="Brand" required>
      <div class="btn-row">
        <button type="submit">Add Vehicle</button>
        <a href="/"><button type="button" class="back-btn">Back to List</button></a>
      </div>
    </form>
  </div>
</body>
</html>